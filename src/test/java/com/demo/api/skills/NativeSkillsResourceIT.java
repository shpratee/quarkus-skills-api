package com.demo.api.skills;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeSkillsResourceIT extends SkillsResourceTest {

    // Execute the same tests but in native mode.
}